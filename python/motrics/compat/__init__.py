"""Drop-in replacements for other MOT metrics libraries, backed by motrics.

Each submodule mirrors the package layout of the library it replaces, so
migrating is a one-line import swap (e.g. ``import motrics.compat.motmetrics
as mm``) rather than a rewrite.
"""
